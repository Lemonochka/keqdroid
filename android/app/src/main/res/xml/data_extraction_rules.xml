<?xml version="1.0" encoding="utf-8"?>
<!-- Backup opt-out (API 31+): server configs carry UUIDs/passwords, so the
     app data is excluded from cloud backups and device-to-device transfer.
     Pairs with android:allowBackup="false" / android:fullBackupContent="false"
     in AndroidManifest.xml, which cover API <= 30. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
