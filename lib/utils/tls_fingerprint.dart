/// Чем клиент представляется серверу, когда ссылка не называет `fp`.
///
/// Не chrome: на REALITY-узлах chrome — 0 успешных запросов из 30, firefox —
/// 30 из 30; сервер принимает соединение и молчит. Фильтр видит только
/// приветствие TLS, а по нему REALITY от обычного TLS не отличить, поэтому
/// выбор один на все протоколы и на оба ядра — иначе смена ядра молча меняет
/// отпечаток.
const String defaultTlsFingerprint = 'firefox';
